import {
    AGREGAR_PELICULA
  } from '../types';

export default (state, action) => {

    switch (action.type) {

        // case xd : 
        //     return {

        //     }


        default:
            return state;

    }

}